import { ActiveuserPipe } from './activeuser.pipe';

describe('ActiveuserPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveuserPipe();
    expect(pipe).toBeTruthy();
  });
});
