import { ServicefilterPipe } from './servicefilter.pipe';

describe('ServicefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
